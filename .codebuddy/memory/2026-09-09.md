## CBDB 帮助文件（xyinde cloud）研读（2026-09-09）
- 用户要求阅读 `https://api.cloud.xyinde.com/cbdb/v3/basicSearch/getHelpFile`。该 URL 返回的是 **《CBDB 在线使用手册》PDF**（引得数字人文平台，运营方北京元引科技 www.inindex.com，2023.9），是**产品使用手册而非 REST 接口规范**。用 pdfjs-dist（--no-save 安装，CMaps 解码中文）成功提取 77 页正文。
- 手册揭示 CBDB 数据模型与 10 类查询（人名/地名/职官/入仕/亲属关系/社会关系/社会关系网络/两人社会关系/社会区分/可视化），与 `scripts/build-characters.mjs` 已从 Harvard `cbdbapi` 提取的字段（BasicInfo 生卒/朝代/别名、PersonAddresses 籍貫、PersonPostings 官职、PersonTexts 著述）完全吻合。
- 关键缺口：`relations.json` 仍为空 `[]`，无 `build-relations.mjs`；`RelationClient` 预期 `Relation{a,b,type,source,detail,dynasty}[]`。CBDB 手册确认含亲属关系（五服：父/母/兄/弟/子/女…）与社会关系（5 大类/400+ 类别：学术/朋友/政治/著述等），正是该字段的数据源。
- 诚实局限：该 PDF 不含 v3 REST 端点具体路径/参数/鉴权/JSON schema（仅 `basicSearch` 路径前缀暗示端点分组）；本项目现用 Harvard `cbdbapi` 而非 xyinde v3。要接入关系数据，可在现有 Harvard API（`PersonKin`/`PersonAssoc`）基础上扩展脚本，无需依赖 v3 spec。
- 清理：解析用的 `.tmp/` 与 `node_modules/pdfjs-dist`（--no-save，未写入 package.json，node_modules 已 gitignore）已处理，未污染仓库。

## 人物考据档案页卡片式布局（2026-09-09，v1.4.3）
- 需求：人物考据档案页增加卡片式显示。真相：`components/CharacterCard` 与 `.character-card` 样式本就是卡片，但 `components/CharacterList.tsx` 外层只用单列 `display:grid; gap:12`（无列数）→ 视觉上是纵向列表而非卡片网格。
- 改动：`app/globals.css` 新增 `.character-grid`（`repeat(auto-fill,minmax(280px,1fr))` 响应式多列、等高等距）；`CharacterList` 容器改用 `.character-grid`，窄屏自动回落单列。版本 patch 1.4.2→1.4.3（package.json + CHANGELOG，无运行时代码版本展示位、无文件头版本注释需同步）。lint 0 错误。

## 合规说明拆分为四个独立页面（2026-09-09，v1.4.3）
- 需求：页脚的「开源协议 / 数据来源 / 免责声明 / 反馈渠道」四项原为 `Footer.tsx` 统一指向 `/help`，且 `help/page.tsx` 只有一个聚合 `#data` 小节。现拆分为独立路由：`/license`、`/data-source`、`/disclaimer`、`/feedback`（均 server component + Metadata，沿用 `.card .stat-panel` 与 breadcrumb 结构，主容器加语义化 id）。
- 内容权威源：`docs/01-项目基础说明/开源合规声明规范.md` + README（GitHub Issues 反馈渠道 `github.com/sutchan/ancient-book-lib/issues`）+ MIT LICENSE。人物考据事实：CBDB 导入 28 位（与内存一致，勿写"关系已导入"）。
- 联动修改：`Footer.tsx` 四项链接改指各自页面；`help/page.tsx` 导语 `/help#data`→`/data-source`，锚点导航 `#data`→`#legal`，原聚合小节重构为「相关说明」卡片引导至四页。
- 版本：沿用本会话已 bump 的 1.4.3（patch，文档/页面类），CHANGELOG 1.4.3 条目增补四页说明。navbar 未加这些页（属页脚/法务页，非主导航）。原型 `prototype/` 为遗留非运行站点，未改。lint 0 错误。

## navbar 加入四页 + 同步原型页脚（2026-09-09，v1.4.3）
- 用户追加：「把这些页也加入 navbar 并同步原型页脚」。
- 真实 navbar：`components/Navbar.tsx` MENU 数组新增 4 项（开源协议/数据来源/免责声明/反馈渠道，key/label 中文），移动端 mobile-menu 因遍历同一 MENU 自动同步。`nav-menu` 为 `display:flex` 无 wrap，11 项在 ≥1200px 桌面可容纳，移动端走汉堡菜单；平板 768–1024 可能略溢出（已提示）。
- 原型同步：新建 4 个独立静态页 `prototype/pages/{license,data-source,disclaimer,feedback}.html`（沿用 help.html 壳，`.card`/`.card-body`/`.card-title`/`.breadcrumb`/`.section-title` 均来自 prototype CSS，内容镜像线上页）；`prototype.html` 页脚 `#page-help`×4 改 `./pages/<页>.html`，9 个 `pages/*.html` 页脚 `./help.html`×4 改各自 `./<页>.html`。原型 nav 仍 7 项（未加 4 页，仅页脚同步，符合"同步原型页脚"诉求）。
- 验证：`#page-help` 仅剩 prototype.html 两处"帮助"导航（正确保留）；Navbar lint 0 错。
