# 2026-09-08

## 复核「去除所有演示数据」现状（v1.3.3）
- 用户再次提出「去除演示数据、直连 daizhigev20 真实古籍」。经核查，该目标在 **v1.3.0 已完成并随 v1.3.2 提交**：`lib/data-generated.ts` 已删；`lib/catalog.ts` 从 `public/index/daizhige-catalog.json`（15,694 部，`npm run build:catalog` 生成）加载真实书目；`components/RemoteReader.tsx` 按需 fetch 上游 raw URL；`app/read/[title]/` 与 `app/book/[id]/` 为删除 demo 阅读器后的空残留目录。
- 运行代码（app/components/lib，排除 test/）中已无任何硬编码 demo 书单/人物/关系；仅剩 UI 示例文案（论语/孔子作为搜索框 placeholder，非数据）。
- 本轮清理残留：① `lib/download.ts` 馆藏导出头部"演示预览版导出"→"馆藏藏书清单导出"；② `lib/types.ts` 类型 `SearchDemoResult`→`SearchTitleResult`；③ 删除两个空残留路由目录。版本 **1.3.2 → 1.3.3**（package.json / README / CHANGELOG）。tsc 通过。
- **唯一仍含 demo 数据处 = `prototype/` 遗留原型站**（`prototype/data/app-data.js` 38 位人物/6 关系、`prototype/assets/js/view-read.js` 硬编码 论语/心经样本），但该目录非运行站点。是否清理 prototype/ 待用户确认（删除涉及 30+ 文件，风险较高）。
- 真实书目文件已落地磁盘：`public/index/daizhige-catalog.json` 含 **15,694 部**，首条 `dzg-00001 一切法高王经` 直链 raw.githubusercontent.com/garychowcmu/daizhigev20/...txt，链路完整。

## 原型审查与改进 v1.3.2 / v1.3.4（本会话，用户口径：保留并持续改进 prototype/）
- **v1.3.2**：修致命回归 `getMain(){ return getMain(); }` 无限递归（v1.2.10 引入，任何重渲染均 RangeError）；书单 onclick 引用 IIFE 私有 `state` → `AB.openBook(id)`；人物卡空字段 `undefined` → 按字段渲染；检索筛选失效 + 每页默认 12 不在选项 → 生效/20/朝代动态；统计页繁简字 429→实际 235（`Object.keys(T2S_MAP).length`）；alert → 页面内 aria-live 结果区 + 关系类型筛选；导航 aria-current、汉堡 aria-expanded、生僻字键盘可达 + Esc 关闭；「无隐私收集」→「仅匿名访问统计」。821 行 `prototype.js` 拆为 `prototype/assets/js/` 10 模块（`window.AB` 命名空间 + 经典 script 保 file:// 直开）。
- **v1.3.4**（会话间隙项目被外部 bump 到 v1.3.3，再次印证动手前实查 package.json）：316 行 `prototype.css` → `prototype/assets/css/` 6 文件（base/components/home/reader/pages/responsive，responsive 最后加载）；**CSS 目录迁移后 hero 背景路径须改 `../images/hero.png`**；补 `:focus-visible` 焦点环 + `.sr-only` + `prefers-reduced-motion`；书单/检索结果 `cursor:pointer`；补 `.tag.on` 选中态；换书重置章节进度（boot.js renderRoute）；删除从未被引用的 `data/{book,search,character}-data.json`；app-data.js 数据版本 2.0→2.2。
- 验证手法（可复用）：临时 node 脚本批量改 HTML 引用 + 抽取 T2S_MAP；自写「悬空引用 + >200 行」校验脚本；**vm + 最小 DOM 桩（假 document/localStorage/IntersectionObserver）跑渲染函数冒烟**，比人工点检可靠；`tsc --noEmit` + `npm test`（17/17）。

## 原型第四轮改进（v1.3.5，改动随 v1.3.4 发布提交一并入库）
- **演示标识（消除误导）**：`core.renderPrototypeNotice()` 用 JS 动态注入顶部「原型」提示条（11 个页面免改 HTML）；阅读页对非《论语》《心经》章节显示「本章为原型演示样张」提示（`AB.isSampleText(book)`）。
- **localStorage 兜底**：`file://` 直开/隐私模式下 `localStorage` 抛异常会让偏好读写整体失效 → 新增 `AB.lsGet/lsSet`（try/catch），state 初始化、`save()`、阅读进度、检索偏好全部改走封装。
- **移动端菜单裁切**：8 个菜单项约 392px，`.mobile-menu.open` 原 `max-height:320px` 吃掉末尾两项 → 480px + `overflow-y:auto`。
- `wireframes.html` 版本标注 V2.0 → V2.2。
- **协作坑（新）**：本轮编辑期间另一会话执行了 `git add -A && git commit`（867d41b v1.3.4 发布），把我未完成的改动连同临时脚本 `scripts/_tmp_smoke.cjs` 一起提交；事后用 `git add -A <file>` 暂存该临时文件删除并补记 CHANGELOG [1.3.5]。教训：① 临时脚本不要放在会被 `git add -A` 扫到的 `scripts/`（或及时删除）；② 提交前必须自己跑 `git status`/`git log` 确认 HEAD 与工作区，勿假设改动未入库。

## 重写帮助页 app/help/page.tsx（用户口径：侧重用户使用帮助 + 扩充 FAQ）
- 旧版偏技术架构（纯静态/数据管理 PRD/为何不用 DB），FAQ 仅 3 条且均为架构向。
- 新版结构：快速上手三步 → 锚点导航 → 一检索 / 二浏览全馆藏 / 三在线阅读（分片懒加载、IDB 缓存100MB LRU、大文件提示、工具栏字号行距简体对照下载、进度保存、划词检索）/ 四繁简与主题 / 五阅读历史与书单导出 / 六考据与统计（待接入）→ FAQ（17 条用户向：如何开始阅读、全文vs标题检索与回退、检索无果、大文件、简体对照是否改原文、进度跨设备、字号行距、下载与书单导出、划词、设置保存、是否免费、手机可用、乱码生僻字、待接入、为何联网/不用DB、版权商用、隐私统计）→ 数据来源与版权隐私。
- 内容均依据实跑代码核对：SearchClient（全文索引缺失回退标题、每页10/20/50）、CatalogInner（防抖250ms、十大藏库标签、导出CSV含直链）、RemoteReader（Range分片>5MB大文件警告、IDB 100MB LRU、ab-remote-/ab-recent/ab-simple/ab-theme localStorage、划词≤30字）、Navbar（繁简/三主题）、download.ts（免责声明、BOM）。
- 纯服务端静态组件，仅替换 JSX，沿用 `Metadata`/`Link` 导入，无逻辑改动。ESLint 未配置（交互提示），未跑 build。
