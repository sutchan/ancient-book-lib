# 2026-09-08

## ancient-book-lib 版本与状态
- 本会话开始时项目 HEAD 已跳至 **v1.2.6**（非前一日笔记假设的 1.0.3），印证「写文档/版本同步前必须 `node -e require('./package.json').version` 实查，勿按旧会话记忆盲写」。务必以 package.json 为权威源。
- 生产站点 https://guji.ewuse.com/ 已上线（EdgeOne Pages，根路径部署）。

## 书目页移除冗余说明文本
- `app/catalog/CatalogInner.tsx`：移除书目页说明中的「原始数据 4.8 GB · 托管于 garychowcmu/daizhigev20 · 本仓库不复制 TXT，阅读时按需加载」整段，仅保留「共 X 部古籍」。改动精确匹配用户给出的字符串，未触碰 README / StatsClient 中措辞不同的同类文本。
- 版本 bump 1.2.6 → **1.2.7**（package.json / README 徽章行 / CHANGELOG 新增 [1.2.7] 小节 + 底部 compare 链接）。无 lint 错误。
- 说明：此前提示的「GA 与零隐私收集冲突」已由用户自行解决（README 现写「仅匿名访问统计，GA4」并标注 G-H76XG9L6FZ），已写入 MEMORY.md，后续不再重复提示。

## 人物功能改为数据驱动（去除示范人物）
- 用户确认口径：人物从 45 部古籍 `books.author` 字段派生，剔除佚名/集体编撰，自动关联著作，缺失字段留空不虚构；relations 仅保留两端均在人物集中的有效引用。
- 实现：`prototype/data/app-data.js` 改为 IIFE，新增 `AUTHOR_PERSONS`（作者串→真实人物名数组）映射，由 `books` 派生 `characters`（id 改 `c001` 格式，按中文排序），并把 `relations` 精简为 6 条（孔子—孟子、老子—庄周、司马迁—司马光、张仲景—李时珍、屈原—司马迁、老子—张仲景）。
- 派生结果：38 位真实人物、6 条关系，关系两端全部在人物集中（无悬空）。
- 清理：`prototype/data/character-data.json`（文档标注的冗余模拟数据，仅 docs 引用）清空并标注废弃；`components/CharacterList.tsx` 改为空字段条件渲染（不再显示「字 」「朝代  · 」空标签）。
- 版本 bump 1.2.7 → **1.2.8**（package.json / README 徽章行 / CHANGELOG 新增 [1.2.8] 小节 + 底部 compare 链接）。`npm run build:index` 重新生成 `lib/data-generated.ts`；`npx tsc --noEmit` 通过。
