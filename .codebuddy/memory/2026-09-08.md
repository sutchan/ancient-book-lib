# 2026-09-08

## ancient-book-lib 版本与状态
- 本会话开始时项目 HEAD 已跳至 **v1.2.6**（非前一日笔记假设的 1.0.3），印证「写文档/版本同步前必须 `node -e require('./package.json').version` 实查，勿按旧会话记忆盲写」。务必以 package.json 为权威源。
- 生产站点 https://guji.ewuse.com/ 已上线（EdgeOne Pages，根路径部署）。

## 书目页移除冗余说明文本
- `app/catalog/CatalogInner.tsx`：移除书目页说明中的「原始数据 4.8 GB · 托管于 garychowcmu/daizhigev20 · 本仓库不复制 TXT，阅读时按需加载」整段，仅保留「共 X 部古籍」。改动精确匹配用户给出的字符串，未触碰 README / StatsClient 中措辞不同的同类文本。
- 版本 bump 1.2.6 → **1.2.7**（package.json / README 徽章行 / CHANGELOG 新增 [1.2.7] 小节 + 底部 compare 链接）。无 lint 错误。
- 说明：此前提示的「GA 与零隐私收集冲突」已由用户自行解决（README 现写「仅匿名访问统计，GA4」并标注 G-H76XG9L6FZ），已写入 MEMORY.md，后续不再重复提示。

## 人物功能改为数据驱动（去除示范人物）
- 用户确认口径：人物从 45 部古籍 `books.author` 字段派生，剔除佚名/集体编撰，自动关联著作，缺失字段留空不虚构；relations 仅保留两端均在人物集中的有效引用。
- 实现：`prototype/data/app-data.js` 改为 IIFE，新增 `AUTHOR_PERSONS`（作者串→真实人物名数组）映射，由 `books` 派生 `characters`（id 改 `c001` 格式，按中文排序），并把 `relations` 精简为 6 条（孔子—孟子、老子—庄周、司马迁—司马光、张仲景—李时珍、屈原—司马迁、老子—张仲景）。
- 派生结果：38 位真实人物、6 条关系，关系两端全部在人物集中（无悬空）。
- 清理：`prototype/data/character-data.json`（文档标注的冗余模拟数据，仅 docs 引用）清空并标注废弃；`components/CharacterList.tsx` 改为空字段条件渲染（不再显示「字 」「朝代  · 」空标签）。
- 版本 bump 1.2.7 → **1.2.8**（package.json / README 徽章行 / CHANGELOG 新增 [1.2.8] 小节 + 底部 compare 链接）。`npm run build:index` 重新生成 `lib/data-generated.ts`；`npx tsc --noEmit` 通过。

## 全项目审查 + 修复（v1.2.9）
- 用户要求「修复所有」。先重查 HEAD 版本为 **v1.2.8**，确认文件实际内容后再动手（避免过时快照）。
- **关键事实（决定 #1/#2/#3 修复方式）**：
  - 内置书/精选典籍（data-generated 45 部）阅读与下载走 `getChapterText()`，该函数**只返回硬编码演示样本**（论语4句/心经5句/其他一律《师说》3句），无真实全文。
  - 远程书（/read/remote，15,694 部）走 `RemoteReader` 整本 fetch 上游 raw URL，**真实可读**。
  - `lib/search.ts` 的 `loadChapterText/fetchChunk/loadInvertedIndex/searchByIndex`（PRD 的 Range 分片 + 倒排索引检索链路）**是死代码**：`public/index/` 已生成 `inverted-index.json` + `chunk-manifest.json` 但仅覆盖 45 部演示书，且无任何 UI 调用；15,694 部远程书无前端索引（受「零复制 4.9GB / 禁后端检索」红线约束，无法落地）。
  - 结论：#1/#2/#3 不能以「伪造 15,694 部毫秒级检索」修复，只能**文案对齐（不误导）+ 保留死代码预埋**。
- **已修复（代码 bug）**：
  - `Navbar.tsx` 全 `<a>` 改 `next/link` + `usePathname` 动态高亮（消除全站整页刷新、修 active 永远 home）。
  - `loadCatalog` 加 jsDelivr/statically CDN 镜像降级（原单点失败会瘫痪全站）。
  - `getSubcategories` 取全部子类（原仅 `subcategories[0]`）。
  - `ReaderClient` 去 `dangerouslySetInnerHTML`，改 React 元素渲染生僻字高亮（防 XSS）。
  - `RemoteReader` 新增 `safeHttpUrl()` 校验，三处 `book.rawUrl` 链接阻断 `javascript:` 等危险协议。
  - `download.ts` `downloadText` 文件名净化（防路径穿越/非法字符）。
  - `SearchClient` `hasMore` 改为仅结果达到 limit 时提示（原恒 true）。
  - `BookActions` + 首页 补充「精选典籍为演示样张、全馆藏按需原文阅读」说明，避免误导。
- **其余**（#12 远程书无静态 SEO 页 / #3 死代码）因规模与红线约束不改，标注为已知限制。
- **版本 bump 1.2.8 → 1.2.9**（package.json / README 徽章行 / CHANGELOG 新增 [1.2.9] 小节）。新增 `LICENSE`（MIT，用户未指定协议，默认 MIT 待确认）。`node node_modules/typescript/bin/tsc --noEmit` 通过（PowerShell 下 `cd /d` 不可用，用 `cd e:\path`）。
- 注：`ReaderClient.tsx` / `RemoteReader.tsx` 实际**无** `// path vX.Y.Z` 头注释（与 memory 旧记「v1.2.8/v1.2.7 头注释」不符），故本次未改头注释，版本同步靠 package.json/README/CHANGELOG。

## 原型审查 + 修复（v1.2.10）
- 用户要求「审查原型设计」。审查 `prototype/`（prototype.html / pages/*.html / prototype.js / css / theme-color.css / data/app-data.js / wireframes.html）。
- **已修复**：
  - 独立页面二次渲染崩溃：`prototype.js` 新增 `getMain()` 兼容 `#main-view`/`#page-body`；搜索/人物检索/繁简切换在 `pages/*.html` 原指向不存在的 `#main-view` 而崩溃或失效 → 已修。
  - 统计数字硬编码与真实派生数据矛盾：数据统计页洞察 + 页脚兜底原写死「21 位/20 条/139 章节」，而 `app-data.js` 派生后实为 38 位/6 条 → 改为动态读 `DATA`。
  - 人物页 `slice(0,8)` 仅显示 8/38 → 改为全量。
  - 导航「十大藏库」→ 统一「十大馆藏」（10 个 html）。
  - `node --check prototype.js` 通过。版本 bump 1.2.9 → 1.2.10（package.json/README/CHANGELOG）。
- **已知限制 / 建议（未改）**：人物卡片字段（字号/籍贯/生卒/官职等）派生后全空，属设计选择（缺失留空不虚构）；`getChapterText` 除论语/心经外均返回《师说》样本，导致阅读页正文失真、全文检索命中虚高（线上已加「演示样张」说明，原型未加）；关系页双人查询用 alert 弹窗体验弱；无障碍缺 aria-current/aria-label。

## 去除演示数据、直连上游全量 txt（v1.3.0）
- 用户要求：去除演示数据，直接关联 `https://github.com/garychowcmu/daizhigev20` 仓库里所有 txt。决策：① 全文检索=构建全量倒排索引（下载 4.9GB 生成索引，作为单独重构建任务）；② 人物/关系=保留入口，无数据显示「数据待接入」。
- 发现项目其实已有真实数据系统：`scripts/build-daizhige-catalog.mjs` → `public/index/daizhige-catalog.json`（15,694 部，raw 直链 + CDN 镜像），`/catalog`→`/catalog/book`→`/read/remote` 已直连上游。待去除的是 `lib/data-generated.ts`（45 部手写 demo）及其被引页面。
- 已删除：`lib/data-generated.ts`、`scripts/build-index.mjs`、`app/book/[id]`、`app/read/[title]`、`components/ReaderClient.tsx`、`BookActions.tsx`、`CategoryDownload.tsx`；`app/category`、`app/book-list` 改写为「已迁移」提示页指向 `/catalog`。
- 重写：`lib/search.ts`（标题检索 + 全量倒排索引全文检索，索引缺失回退标题）、`components/SearchClient.tsx`（去 demo 全文样本，接真实目录）、`app/page.tsx`（改用静态 `lib/categories.ts`）、`Footer`/`Navbar`/`StatsClient`/`CharacterList`/`RelationClient`、字符页/统计页改为真实目录与空态占位。
- 新增：`lib/categories.ts`（十大馆藏静态 meta）、`scripts/build-fulltext-index.mjs`（`npm run build:fulltext`，下载上游 TXT 生成 `public/index/fulltext-index.json`）。
- 脚本：`package.json` 移除 `prebuild:index`（会阻断 next build）/ `build:index`，新增 `build:fulltext`；版本 1.2.10 → **1.3.0**；`.github/workflows/deploy.yml` 移除 `npm run build:index` 步骤。
- 验证：`npx tsc --noEmit` 已修两处 Set/Map 迭代类型错误（改 Array.from）；`npm test` 17/17 通过；`next build` 环境判定耗时未在此环境运行，需用户在本地/CI 实跑确认。
- 说明：全量倒排索引构建（4.9GB 下载）尚未实跑，仅脚本+检索接线就位、标题检索立即可用；真实阅读/检索/浏览已全量直连上游。

## 四项高优功能落地（v1.3.1）
- 用户选定全部四项并行推进：① 超大书 Range 分片（P0）、② 全文检索构建（P0）、③ 人物/关系 CBDB（P1，依赖外部）、④ 馆藏/批量下载（P1）。
- **① Range 分片**：`lib/fetchWithTimeout.ts` 加 `range` 选项 + `fetchRangeText`；新增 `lib/remoteBook.ts`（读 `chapters.json` 清单 + 按字节区间拉章节，CDN 镜像优先）；`components/RemoteReader.tsx` 重写为清单驱动分片加载，拆出 `ReaderToc.tsx`/`ReaderToolbar.tsx`（单文件≤200 行）；无清单书目回退整本下载 + IndexedDB 缓存。
- **② 全文检索构建整合**：`scripts/build-fulltext-index.mjs` 在同一次下载 pass 顺带生成单文件 `chapters.json`（替代原设想的每书一个文件，避免 15,694 小文件入库）；验证：前 10 本中 3 本检出章节（dzg-00006 有 59 章）。`fulltext-index.json`/`chapters.json` 加 `.gitignore`（不入库，部署期生成）；`deploy.yml` 加 build:fulltext 步骤。
- **③ 数据驱动接线**：`CharacterList.tsx`/`RelationClient.tsx` 改读 `public/index/characters.json`/`relations.json`（新建空种子），空时回退「待接入」；**数据源未定**，待用户选 CBDB/ctext 后写 ingest。
- **④ 馆藏下载入口**：`lib/download.ts` 新增 `exportBooklistCsv`；`app/catalog/CatalogInner.tsx` 加「导出书单（含原文直链）」按钮，零复制导出当前筛选结果。
- **章节识别修复**：补「品第X/第X品」正则 + 放宽上下文（标题后接缩进正文即可），修复佛经/子书漏检；`chapterBoundaries` 改为基于原始文本字符偏移累加（兼容 \r\n），构建脚本据其换算字节偏移。
- 版本 bump **1.3.0 → 1.3.1**（package.json / README 第 16 行 / CHANGELOG 新增 [1.3.1]）。源码无 `// path vX.Y.Z` 头注释，故未改头注释。`node node_modules/typescript/bin/tsc --noEmit` 通过（exit 0）。
- **关键部署真相**：`fulltext-index.json` 等未被 git 追踪（体积大、生成于部署期，从不入库）。生产站是 EdgeOne Pages，其独立构建默认只跑 `npm run build`，不会运行 `build:fulltext`——故上线全文检索/Range 分片前，**EdgeOne 构建命令须设为 `npm run build:fulltext && npm run build`**（已写入 deploy.yml 的 Actions 步骤，但 EdgeOne 自身步骤需用户在控制台配置）。

## 原型二次审查 + 修复（v1.3.2）
- 用户「继续改进」→ 继续审查 `prototype/`。发现 **v1.2.10 引入的致命回归**：`prototype.js` 的 `getMain()` 被写成 `function getMain(){ return getMain(); }`，无限递归，任何重渲染（繁简切换/翻章/检索/影像对照）均 `RangeError` 崩溃。
- 同时修复：书单 onclick 引用 IIFE 私有 `state`（ReferenceError）→ `AB.openBook(id)`；人物卡片空字段渲染 `undefined` → 按字段存在性渲染；检索页馆藏/朝代筛选形同虚设 + 每页默认 12 不在选项中 → 真实生效、默认 20、朝代选项动态派生；统计页繁简映射字硬编码 429（实际 235）→ `Object.keys(T2S_MAP).length`，删死变量 `era`；双人关系查询 alert → 页面内 `aria-live` 结果区 + 关系类型筛选；高亮对转义关键词切片；零命中回退示例时标注「未命中」；导航 `aria-current`、汉堡菜单 `aria-expanded`、生僻字键盘可达 + Esc 关闭；帮助页「无隐私收集」改为「仅匿名访问统计」对齐 GA4 口径。
- 拆分：821 行 `prototype.js` → `prototype/assets/js/` 10 模块（`t2s-map/core/view-home/view-read/view-search/view-character/view-relation/view-help/view-stats/boot`），用 `window.AB` 命名空间 + 经典 script（保留 file:// 直开），11 个 HTML 引用同步；各文件 ≤200 行。
- 验证：临时脚本抽取 T2S_MAP 并批量改 HTML；`node --check` 全通过；写最小 DOM 桩（vm + 假 document/localStorage）跑通 8 个渲染函数 + 阅读页 + 作废 ID 模式，人物页无 undefined；`tsc --noEmit` 通过、`npm test` 17/17。
- 版本 bump **1.3.1 → 1.3.2**（package.json / README 第 16 行 / CHANGELOG [1.3.2] + 补 [1.3.1][1.3.2] compare 链接；历史条目中的 `prototype.js` 引用保留不改）。
