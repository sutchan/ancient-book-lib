# 2026-09-07

## ancient-book-lib（古籍通 AncientBook）文档与社区健康文件建设

项目事实（实测）：GitHub owner = sutchan，`https://github.com/sutchan/ancient-book-lib.git`；Next.js 14 + React 18 + TS，纯静态 SSG（`next.config.mjs` 开启 `output: "export"`，产物 `out/`，故 `next start` 不适用，用 `npx serve out` 预览）；无数据库、无 ESLint 配置、无测试框架（`npx tsc --noEmit` 是可靠门禁）；目录为根级 `app/ components/ lib/ scripts/ prototype/ docs/ public/`（注意：docs/02 的目录结构规范写的是 `src/app` 旧结构，与实际不一致，未改）；数据源单一源 `prototype/data/app-data.js` → `npm run build:index` 生成 `lib/data-generated.ts`。

本次工作：
- 新建 `.github/` 社区健康文件：CONTRIBUTING.md、CODE_OF_CONDUCT.md、SECURITY.md、SUPPORT.md、PULL_REQUEST_TEMPLATE.md、ISSUE_TEMPLATE/{bug_report.yml, feature_request.yml, content-errata.yml, config.yml}。未创建 `.github/readme.md`（全局规则禁止），未建 FUNDING.md（项目公益非商用、不接受赞助）。
- 新建根 `CHANGELOG.md`（Keep a Changelog + 底部 releases 锚点）。
- 更新根 README：徽章、真实目录结构、完整命令、社区入口、合规说明。
- 回填占位符：`docs/07-环境搭建手册/本地开发环境搭建FAQ.md` 中 `yourname` → `sutchan`，并补社区文档入口；`docs/01-项目基础说明/README-项目简介.md` 补「参与共建」。
- 版本 1.0.0 → 1.0.1（package.json）。

遗留/待确认：仓库尚无 LICENSE 文件，README 仅声明「非商用公益开源」。选择许可证属法律决策，未擅自创建，已在回复中请用户确认（建议 MIT 代码 + 内容 CC BY-NC 4.0 或统一 CC BY-NC-SA 4.0）。
