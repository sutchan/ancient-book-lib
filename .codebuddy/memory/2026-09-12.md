# 2026-09-12

## 人物考据功能完善：人物库检索能力升级（CHANGELOG 记为 1.11.0 草稿）
- 背景：HEAD = `8347ee9`（1.10.0 CBDB 科舉 + 史料來源），`package.json` 仍 1.4.5，CHANGELOG 用 `## [未发布]` 收 1.5.0+ 草稿（不 bump package.json）。人物考据已从 28 人扩到 CBDB 661,350 人（`/people` + `/people/detail`）。
- 本轮改动：
  1. `lib/cbdb.ts` `searchPersons` 由「仅 `startsWith` 前缀」升级为**多级命中**：姓名前缀 > 姓名任意字子串 > 别名字号前缀 > 别名字号子串，前一级不足 limit 才由后一级补。修了「搜姓名中间字（軾→蘇軾）全落空」——人物库空态却提示「可尝试输入姓名中的任意字」，承诺与实际不符。
  2. 移除 `qNorm !== q` 守卫（姓名 + 别名两处）。**该守卫是系统性陷阱**：查询本身是简体时 `qNorm === q` → 归一化副本比对被跳过 → 简体查询永远打不中繁体原名。`lib/catalog.ts` `searchCatalog` 此前踩过同一个坑（已于 09-10 后修掉，P0-3 用例现已转绿）。
  3. `app/people/PeopleInner.tsx`：搜索结果展示别名命中（蘇軾（東坡居士））；达 SEARCH_LIMIT=100 时提示细化关键词；搜索模式下禁用朝代筛选并说明（结果来自人名索引无朝代字段，此前静默失效）。
  4. `app/people/detail/PeopleDetailInner.tsx`：修「生平任职」区块注释误写「人物关系」。
  5. 新增 `test/cbdb-search.test.ts`（10 用例）。全量 `npm test` 40/40 通过，`npx tsc --noEmit` 通过。

## 关键经验
- **写测试比读代码更容易发现「守卫类」逻辑缺陷**：本轮 2 个繁简用例失败才暴露出 `qNorm !== q` 守卫被我从旧实现沿用下来。凡「归一化副本 + 判断是否需要归一化」的写法，一律不要加 `qNorm !== q` 这类守卫。
- `searchPersons` 依赖 `lib/cbdb.ts` 的模块级缓存（searchCache/normSearchCache/altname*），单测须在 `before()` 里装 fetch 桩并全程保留；node:test 每文件独立进程，故缓存不跨文件污染。
- `test.after()` 不是合法 API，须用顶层 `after()`。

## 待用户确认（本轮未动）
- `/character`（旧 28 人 `characters.json` + CharacterList）与 `/people`（CBDB 661,350 人）在导航栏**并存两个入口**（「人物考据」/「人物库」），数据口径不一致。是否将 `/character` 合并/重定向到 `/people`？属产品决策，未擅自处理。
- 人物库按姓氏浏览仅展示 meta 前 60 个姓氏（`meta.surnames` 存 200 个，实际数千），长尾姓氏无入口；搜索结果上限 100 且不支持朝代筛选（已明确禁用而非静默）。

## 愉悦体验层（Delight Layer）落地（原型先行 → 正式站）
- 用户诉求：功能完备但体验不够惊艳；以愉悦体验设计师身份出方案「纸墨雅趣」，先改原型再同步正式站。
- 8 触点：①主按钮水光扫过 ②卡片朱砂点睛线（.card/.character-card::after）③书目行朱笔眉批（.book-item::before）④Hero 光晕漂移 heroDrift ⑤空态图标摇头 iconPeek ⑥随机卡 rc-swap 重放（React key=idx）⑦阅读卷首滚动进度条（fixed 3px 朱砂条）⑧页脚彩蛋（2s 内连点 .footer-stats 5 次 → 篆字飞舞 + toast「文脉绵延，与君共读」，第 3 次点击渐进提示「再点两下，有惊喜…」）。另：检索空态愉悦文案 + 真实可点建议词（仁/君子/天下 → /search?q=&mode=full）；404 页双 CTA；阅读加载/失败文案微调。
- 落点：prototype/assets/css/components.css 末段 + js/core.js（AB.bindScrollProgress/AB.bindEasterEgg，refreshScrollProgress 由 boot 在 renderRoute/initStandalone 后调用）+ js/boot.js；app/globals.css 末段同款 CSS；新组件 components/ScrollProgress.tsx、components/EasterEgg.tsx；Footer（挂 EasterEgg + title 提示）/RemoteReader（挂 ScrollProgress）/RandomBook/RandomCharacter/SearchClient/not-found/read/remote 接线。
- 无障碍红线：全部动画在 @media (prefers-reduced-motion: no-preference) 内；彩蛋对 reduce 用户仅出 toast；toast 用 role=status；进度条 aria-hidden + pointer-events:none。
- 验证：npx tsc --noEmit 通过；node --check 原型 JS 通过；npm run build 成功（33 静态页）。
- ⚠️ 工作区存在并行会话改动（lib/cbdb.ts、app/layout.tsx、catalog、StatsClient、test/cbdb-search.test.ts 等非本任务文件），未触碰未提交。
- 环境坑：本会话 Bash shim 缺 dirname/head/cat/ls 等 coreutils（git/node 可用）；文件读写一律用 Write/Edit 工具，勿用 heredoc 追加。

## 原型↔项目代码同步 + 文档更新（愉悦体验层收尾）
- 同步缺口已修：原型 `prototype/assets/js/view-search.js` 检索空态仍是旧「未找到相关内容」，正式站早已改为「未寻得此卷」+ 可点建议词。已把原型空态对齐：渲染 `.empty-suggest` 建议词（仁/君子/天下）+ 绑定点击触发检索（`AB.state.searchKeyword` + `renderSearch`，mode=full）。
- 正式站 `app/globals.css` 此前缺 `.empty-suggest` 芯片样式（建议词是裸链接），已补，与原型 `components.css` `.empty-suggest` 一致。
- 文档：① CHANGELOG 新增 `[1.12.0]`（未发布区顶部）八触点+同步契约+无障碍红线；② README「功能特性」补「愉悦体验」条；③ 新增 `docs/05-设计规范与原型/愉悦体验层-纸墨雅趣.md`（理念/八触点表/原型↔代码同步契约/无障碍红线/扩展指引）。
- 验证：原型 `view-search.js` `node --check` 通过；本回合仅 CSS + 原型 JS 改动，未动 tsx，不影响 Next 构建。

## 原型↔应用导航对齐（人物库）
- 缺口：并行会话的 CBDB 工作给正式站新增「人物库」路由 `/people`（Navbar 7 项含 人物库），但原型导航只有 6 项、缺「人物库」入口、无 people 页、页脚也无该链接。
- 对齐动作：① 新增 `prototype/assets/js/view-people.js`（`AB.renderPeople`，复用 `AB.DATA.characters` demo 数据，定位为「按姓名/字号检索浏览」的人物库）；② `boot.js` 的 `PAGE_RENDERERS` 注册 `people: AB.renderPeople`；③ 新增独立页 `prototype/pages/people.html`（克隆 character.html，data-page=people，含 view-people.js）；④ `prototype.html` 脚本区引入 view-people.js，主导航+移动菜单+页脚均插入「人物库」（导航 #page-people，页脚 ./pages/people.html）；⑤ 13 个独立页经 Python 脚本批量对齐导航(main+mobile)与页脚，顺序对齐 app（页脚 数据统计→人物库）。
- 结果：原型导航现为 7 项（首页/全馆藏/检索/人物库/人物考据/社会关系/帮助），与 `components/Navbar.tsx` 完全一致；页脚链接（开源协议/数据来源/免责声明/反馈渠道/数据统计/人物库）与 `components/Footer.tsx` 一致。
- 验证：node --check 通过 view-people.js、boot.js；grep 确认所有原型页含「人物库」且换行格式正确。
- 注：原型页脚仍显示 demo 数字（精选 45 部 / 人物 38 位 / 关系 6 条），与 app 真实数字（661,350 人 CBDB）不同——属原型 demo 数据固有差异，未伪造真实数据。

## 项目开发进度分析（2026-09-12 盘点）
- 现状：72 commits，HEAD=`f187d4e`(人物库浏览页)，`git status -sb` 显示 `ahead 1`（1 个 commit 未推送 origin/main）。工作区干净，仅 `.codebuddy/memory/2026-09-12.md` 被其他并行会话改动（非代码）。
- **版本号严重错位（P0）**：package.json 仍是 `1.4.6`，但实际功能已做到 CHANGELOG 的 `1.13.0`（CBDB 人物库全量串联 1.5→1.13）；CHANGELOG 顶部「未发布」区仍把 1.5.0-1.8.0 标为草稿，实则全部已提交。需一次性 bump 版本 + 归并未发布区。
- **relations.json 死文件遗留（P2）**：`public/index/relations.json` 仍为 `[]`；自 v1.6.0 起关系数据已改用 `lib/cbdb.ts` 真实 CBDB 接口（亲属 56.1 万 / 社会 19 万），人物详情关系区块已真实渲染，relations.json 仅剩空种子——应清理或加说明，避免误导。
- **文档滞后（P1）**：任务清单仍停在 V1.0 精简版，未反映 1.1-1.13 的 CBDB/可视化/愉悦层迭代；建议补「迭代进度」小节或新开迭代清单。
- 完成度：V1.0 核心（浏览/检索/双语阅读/分片/考据/下载/合规/GitHub Pages）早已闭环；CBDB 人物库、人物关系、数据统计可视化、愉悦体验层均已实质完成（测试 40/40 绿、build 33 页成功）。原型与 app 导航/愉悦层已双向对齐。
- 建议下一步：① bump 版本至 1.13.x 并归并 CHANGELOG 未发布区；② 清理 relations.json 死文件；③ push 未推送的 1 个 commit；④ 更新任务清单反映迭代进度。

## 剩余任务收尾（v1.13.1 一致性清理）
- 校正：并行会话已把 package.json bump 至 1.13.0（HEAD=`172a5e6` 补全 robots.txt + 时间轴可测试化 + 任务清单同步），且 `main` 与 `origin/main` 已同步（无 ahead/behind）——故进度分析里的「P0 版本错位」「P2 未推送」已被并行会话解决，仅余文档/死文件缺口。
- 本次落地（未提交，待并行会话/用户统一提交）：① CHANGELOG 顶部 `[未发布]` 区两条「愉悦体验层·纸墨雅趣」「人物库检索能力升级」（原标「已发布未单列版本号」+ 过时「1.4.6/草稿」注释）归并为正式条目 `## [1.13.1] - 2026-09-12`，并修正底部 `[未发布]` 比较链接为 `compare/v1.13.1...HEAD`、补 `[1.13.1]` 链接；② package.json `1.13.0 → 1.13.1`（与 CHANGELOG 对齐）；③ `git rm public/index/relations.json`（197KB 死种子，全代码零引用，关系数据走 `lib/cbdb.ts`）；④ 修 `docs/02-架构与开发规范/项目架构设计文档.md` 第 69 行过时引用（relations.json 空种子待 CBDB 接入 → 现状：考据读 characters.json、关系走 lib/cbdb.ts）。
- 验证：`npx tsc --noEmit` 通过（`=== TSC_PASS ===`）；改动均为文档 + 移除未引用文件，不影响构建（build 此前已在 33 页 + sitemap 验证通过）。
- 红线执行：未 `git add -A && commit`，避免与并行会话混改；改动以 staged/modified 形态留在工作区，由统一流程提交。
