# 2026-09-13

## 人物考据并入人物库 — 收尾核验
- 复核确认：到 v1.14.1（2026-09-12）考据工作台 10 个死文件（CharacterDossier/CharacterProfileView/CharacterList/CharacterCard/characterProfile/characterValidate/characterFilter + 2 test + 规格文档）已彻底删除，对 app/components/lib 零引用。当前 package.json=1.14.1，git 工作树干净。
- 全仓 grep + 目录列举复核：components/lib/test 均无 Character*/character* 残留；app/page.tsx（学术工具仅剩人物库/社会关系/数据统计）、app/data-source/page.tsx、app/help/page.tsx、prototype/pages/character.html（data-page=character-guide）合并改动均到位。
- 门禁复跑：npx tsc --noEmit 0 错误；npm test 39/39 全绿。合并任务 100% 完成，无悬挂引用。sitemap 仍收 /character/ 属预期（与 /category、/book-list 一致，无单页排除机制）。

## 清理超体积 MEMORY.md
- 按系统要求将超限（被截断注入）的 MEMORY.md 重写为精简版：合并重复项、移除过时的「人物/关系旧架构（CharacterList 等）」细节，保留稳定跨会话事实（仓库/部署、数据单一源、版本与提交规则、工程环境坑、原型结构、本地记忆目录约定、人物考据合并结论、待办）。

## 目录规范校正 + 原型死代码清理（v1.14.2，2026-09-13）
- ① 文档债：重写 docs/02-架构与开发规范/项目目录结构规范.md 目录树为真实根级 app/ 结构（非 src/app），补 components/lib/public/index，删不存在的 src/hooks、src/styles、tailwind.config.js，next.config.js→next.config.mjs；目录职责说明 src/static-index→public/index。
- ② 原型清理：删 prototype/assets/js/view-character.js（人物考据视图，已并入 /people）；boot.js 移除 PAGE_RENDERERS.character 渲染器（模块注释 v2.2→v2.3）；用一次性脚本清除 15 个 HTML（prototype.html + 14 个 pages/*.html）残留的 `<script src=".../view-character.js">` 引入；view-people.js 悬空注释改正。AB.DATA.characters 数据源在 data/app-data.js，删除不影响 view-people.js。
- 版本 bump 1.14.1→1.14.2（package.json + CHANGELOG 新增 1.14.2 小节与底部比较链接）。README 无版本徽章故无需改；无 metadata.json/i18n appVersion。
- 验证：node --check boot.js 通过；全仓 grep view-character 仅余 view-people.js 一处已修正的描述性注释；临时脚本已自删（放仓库外 e:/Github/）。

## 待用户指定下一步
- ①②③ 均已闭环。若继续，候选：修 LICENSE（MIT 待确认是否改协议）、或其它新需求。

## 书签功能实现（v1.14.3，2026-09-13）
- 执行 `docs/04-开发任务清单/书签功能开发任务清单.md`（用户 @ 该文件要求「执行开发任务」）。当前 package.json 已预置 1.14.3（会话间隙被外部改动；CHANGELOG 1.14.3 节已含任务清单文档+原型对齐），故沿用 1.14.3，未另开 1.14.4。
- 新增：`lib/bookmarks.ts`（收藏 + 阅读位置书签 + subscribe/notify pub/sub，window 守卫+容错）、`lib/useBookmarks.ts`（hook，监听 storage 事件跨标签页同步）、`app/bookmarks/page.tsx`（我的书架+导入/导出 JSON）、`test/bookmarks.test.ts`。
- 改动：`components/Navbar.tsx`（MENU 加「书签」+ 收藏数角标 id=nav-bookmarks）、`app/catalog/CatalogInner.tsx`（卡片星标 id=bookmark-toggle-${id}）、`app/catalog/book/CatalogBookInner.tsx`（详情页收藏按钮）、`components/RemoteReader.tsx`（收藏切换 + 阅读位置书签面板）。
- 验证：`node node_modules/typescript/bin/tsc --noEmit` 0 错误；`npm test` 47/47 全绿（新增 8 书签用例）。被改文件头注释升至 v1.14.3；CHANGELOG 1.14.3 补「新增（书签功能实现）」小节；任务清单勾选为已完成。
- 注意：git 工作树现状较乱——HEAD=bf08411(v1.14.1)，但 package.json/CHANGELOG 有未提交的 1.14.2 目录规范 + 1.14.3 任务清单/原型对齐 + 本次书签实现。提交时由用户/会话决定如何归并这些未提交改动。
- 后续（同日）：书签功能已被并行会话提交并推送，HEAD 现为 `175d647 feat(bookmarks): 实现书籍收藏与阅读位置书签`；85 提交、工作树干净、与 origin/main 同步、36 标签止于 v1.14.0。

## 剩余开发任务清单（v1.14.4，2026-09-13）
- 核实进度并新增文档后 bump：1.14.3 → **1.14.4**。现状：HEAD `175d647`、85 提交、47 用例通过、`tsc` 0 错误、与 origin 同步、工作树干净；Git 标签止于 v1.14.0（v1.14.1–v1.14.3 未打标签）。
- 新增 `docs/04-开发任务清单/剩余开发任务清单.md`：汇总 **27 项**剩余任务——P0 R1–R5（阅读器内搜索 / 引用格式复制 / 检索结果定位跳转 / 移动端阅读优化 / 帮助页陈旧文案）；P1 R6–R13（批注高亮 / 笔记 / 专题文集与主题书单 / 名句典故库 / 版本校勘记 / PWA 离线 / 阅读统计 / 学术数据导出）；P2 R14–R23（跨典籍关联检索 / 时代文史 / 高频词 / 国学应试 / 精细导出 / 多语言 / TTS / 知识图谱 / 社区校勘 / 纯前端 AI）；工程与文档债 R24–R27（补标签 / 原型同步「书签」 / 刷新进度清单 / 校正 .github 文档）。
- 来源：`docs/10-用户研究与需求洞察/03-需求洞察与优先级报告.md`（D1–D15）、`docs/03-产品需求PRD/古籍资源增量价值补充.md`（2.1–2.8）、`docs/04-开发任务清单/V1.0精简版开发任务清单.md` §6、源码实测。
- 附带发现的文档债：`.github/CONTRIBUTING.md`/`SUPPORT.md`/`PULL_REQUEST_TEMPLATE.md` 仍引用已删除的 `npm run build:index` 与 `lib/data-generated.ts`（v1.3.0 起移除）；`app/help/page.tsx` 第 125/173 行仍有「待接入/占位」陈旧文案；`prototype/` 导航仍 6 项未含「书签」。
- 注意：期间并行会话在改 `README.md` 与 `docs/01-项目基础说明/*`（加书签特性条目），非本次改动，勿覆盖。

## 推进剩余任务（v1.15.0，2026-09-13）R1–R5 + R24/R25
- 执行《剩余开发任务清单》建议范围 v1.15.0。核实：**并行会话已完成 R5**（帮助页去「待接入」+ 补书签说明，头注释 v1.14.4）、**R26**（刷新开发进度清单）、**R27**（.github 文档 `build:index`→`build:catalog`、`data-generated.ts`→`public/index`）——未重复触碰这些文件。
- 本次实现（version 1.14.4 → **1.15.0**，minor）：
  - **R1 阅读器内页面搜索**：新增 `lib/readerSearch.ts`（`buildReaderMatches`/`splitHighlight`，繁简互通）+ `components/ReaderSearchBar.tsx`；`RemoteReader` 加 showSearch/submittedQuery/matches/jumpToMatch/renderText(`<mark>`)；分片模式检索当前章节、整本模式检索全书。
  - **R2 引用一键复制**：新增 `lib/citation.ts`（`formatCitation`/`copyText`，Clipboard API + execCommand 降级）；划词浮层抽为 `components/ReaderSelectionMenu.tsx`，加「复制引用」+ toast。
  - **R3 检索结果定位**：`SearchClient` 结果链接带 `?q=`（仅全文模式）；`app/read/remote/page.tsx` 读 `q` 传 `initialQuery`，阅读器自动打开页内搜索列命中。注：倒排索引为「词→书ID」**书级粒度、无章节**，章节级直达需后续章节感知索引（已在 CHANGELOG/清单注明）。
  - **R4 移动端**：正文左右滑动翻页（onTouchStart/End，阈值 50px）、进度条 `range` 拖拽跳章（`goPage`）、`<420px` 自动字号 15；`globals.css` 加 `touch-action:pan-y`、`.reader-progress-slider`、≤480px 响应式。
  - 新增 `test/reader-tools.test.ts`（6 用例）。验证：`tsc` 0 错误；`npm test` **53/53 全绿**。
  - **R25 原型导航同步「书签」第 7 项**：新增 `prototype/pages/bookmarks.html`（静态演示）；用临时脚本（`scripts/tmp/`，已删）给 14 个 `pages/*.html` + `prototype.html` 的导航与移动菜单插入书签项；`docs/05` 原型页面清单补「书签页」（其 7 项导航描述并行会话已改）。
  - **R24 标签**：本地补 `v1.14.1`→`bf08411`、`v1.14.3`→`175d647`（annotated）；`v1.14.2` 无对应提交（与 1.14.3 同批、package.json 已 1.14.3）不虚标。**未推送**，待 `git push origin v1.14.1 v1.14.3`（远端已有 36 tag）。
- 未提交（工作树同时含并行会话大量文档改动）。后续：R6–R13（v1.16.x 研习）、R14–R23（v1.17+ 学术/平台）。
- 更新：随后并行会话把 v1.14.4+v1.15.0 全部改动提交为 `41b36d5 docs: 同步 v1.15.0 与 v1.14.4 变更至文档与社区配置`（HEAD 41b36d5，版本 1.15.0）。

## 全站页面文案精简（v1.15.1，2026-09-13）
- 任务：检查应用所有页面，去除非必要文字、精简冗余文字。逐页读取 `app/**/page.tsx` 与关键组件（Footer/CatalogInner/CatalogBookInner/RemoteReader/SearchClient/StatsClient/RelationClient/PeopleSearchResults/PersonTimeline/RandomBook/RandomCharacter/ReaderToolbar/EasterEgg）。
- **删除（非必要/失效）**：首页与 hero-sub 重复的说明句；**页脚已废弃的「统一解压密码 / 备用网盘公示」**（现行方案=上游直链+免责声明，无网盘环节 → 属失效文案）。
- **精简**：`data-source`（去重「可溯源、不臆断」、压缩字段细目与 `          ` 排版空隙）、`disclaimer`（去引导赘句）、`feedback`（去客套语、重复措辞）、`license`（去元叙述句）、`character`（去与按钮重复的引导句）、`category`/`book-list`（旧引导页）、`bookmarks`。
- 改动 10 文件：`app/page.tsx`、`components/Footer.tsx`、`app/{category,book-list,character,data-source,disclaimer,feedback,license,bookmarks}/page.tsx`；被改文件头注释统一 `v1.15.1`。version 1.15.0→**1.15.1**，CHANGELOG 加 `[1.15.1]`。验证：tsc 0 错误 / lint 0 / npm test **53 全绿**。
- 原则：保留全部法律声明、数据溯源与授权实质内容，仅删重复与虚词（未删 help/stats/relation 的实质说明）。
- **附带发现的真实 bug（待用户决定，本次未改）**：`components/RandomCharacter.tsx` 仍链接 `/character#character-{id}`，但 `/character` 现已是引导页、无此锚点 → 应改指 `/people/detail?id={id 去 cbdb- 前缀}`（与 `app/character/page.tsx` 一致）。

## 书签功能开发任务清单（v1.14.3，2026-09-13）
- 用户要求「给应用增加书签功能，给出开发建议」→ 先给分阶段方案（A 书籍收藏 MVP / B 阅读位置书签增强），再「生成任务清单并更新到项目文档」。
- 新增 `docs/04-开发任务清单/书签功能开发任务清单.md`：分 Phase 1（收藏）/ Phase 2（阅读位置）/ 跨设备（导出导入 JSON）三块任务清单，含数据模型（Bookmark / ReadPosBookmark）、localStorage key 约定（ab-bookmarks / ab-pos-${bookId}）、改动文件映射、验收标准与风险。
- 架构约束：纯静态零后端，书签仅存 localStorage；复用现有 `ab-recent` / `ab-remote-${id}` 模式；不引新依赖，跨组件实时同步用 `lib/bookmarks.ts` 内置极简 subscribe/notify；UI 须 "use client" + useEffect 后渲染（参考 RecentBooks）。
- 版本 bump 1.14.2→1.14.3（package.json + CHANGELOG 新增 1.14.3 小节与底部比较链接）。尚未编码，等待用户确认是否实现 Phase 1。

## 文档全量同步（v1.14.4，2026-09-13）
- 用户要求「更新所有项目文档」。盘点发现**并行会话已完成**：① 书签功能完整实现（lib/bookmarks.ts、lib/useBookmarks.ts、app/bookmarks/、Navbar 7 项、CatalogInner 星标、CatalogBookInner 按钮、RemoteReader 工具栏+阅读位置书签、test/bookmarks.test.ts）；② package.json 已 bump 至 **1.14.4**；③ 新增 `docs/04-开发任务清单/剩余开发任务清单.md`（R1–R27，含 R24 补标签 / R25 原型书签 / R26 刷新进度清单 / R27 .github 陈旧引用）。
- 本轮同步（计入 CHANGELOG [1.14.4]）：
  - 《开发进度清单》全量刷新 v1.14.0→v1.14.3：测试 39→**47**（8 测试文件），新增「书签」功能小节，遗留清单新增「原型未同步书签」「馆藏古籍关联人物规划」；不再写死易失真的 HEAD/提交数。
  - 9 份规范文档「适配代码版本」v1.3.6/v1.3.7→**v1.14.3**（校订 2026-09-13）：项目整体概述 / README-项目简介 / 项目品牌定名规范 / 开源合规声明规范 / 项目架构设计文档 / 代码开发规范 / 性能优化开发规范 / 资源分片请求规范 / 静态索引预处理规范。
  - 修正陈旧：人物考据「数据待接入」→ CBDB 661,350 已接入；索引路径 src/static-index/→public/index/（部署规范 / 环境FAQ / 静态索引规范）；代码开发规范去掉不存在的 TailwindCSS；架构文档 §5 表头 v1.3.6→v1.14.3。
  - 功能特性补书签：README / 项目整体概述 / README-项目简介 / 架构文档展示层 / 开源合规隐私条款；导航 6→**7 项**（UI与UX全局设计规范）；版本迭代规范标注收藏已在 v1.14.3 落地。
  - **R27**：.github/CONTRIBUTING.md、SUPPORT.md、PULL_REQUEST_TEMPLATE.md 的 `npm run build:index`→`build:catalog`、`lib/data-generated.ts`→`public/index/`；CONTRIBUTING 数据来源章节 + 目录树更新。
  - **R5**：app/help/page.tsx 移除「待接入占位」文案 + FAQ，新增书签使用说明（头注释 v1.4.3→v1.14.4）。
  - 剩余任务清单 R26 标记已完成；09 审计报告「后续更新」块、10 用户研究 README 加现状对齐（历史档案保留）。
- CHANGELOG 出现**同版本号重复小节**（并行会话各写一份 [1.14.4]），已合并为单节 + 两个子标题；`npm run sync:changelog` 重跑（1.14.4 未 commit/无 tag → 属「无落点」不生成链接，与 1.14.2 同属正常）。
- 门禁：tsc 0 错误、npm test 47/47。
- 教训（重要）：并行会话在同一轮内高频改动（新增文档、bump 版本、写 CHANGELOG、跑脚本）；动手前及每次写入前**必须重读真实文件**，`write_to_file` 覆盖类操作风险高（本轮曾按旧快照准备 bump package.json，实已被对方改为 1.14.4）。

## 原型对齐应用代码与规范文档（2026-09-13）
- 用户指令「对齐原型和应用代码和规范文档」。侦察确认权威事实：`components/Navbar.tsx` MENU 仅 **6 项**——首页 / 全馆藏 / 检索 / 人物库 / 社会关系 / 帮助（`/character` 为「已并入人物库」引导页、`/category`、`/book-list` 为全馆藏引导页，均不在 nav；数据统计仅 Footer 链接）；`app/page.tsx` 学术工具=人物库/社会关系溯源/数据统计，且含 Hero CTA 双按钮 + 趣味探索（RandomBook+RandomCharacter）。
- 原型此前 nav 错乱（多会话叠加：出现「人物考据+人物库」并存或缺「数据统计」）。本轮用一次性脚本将 **全部 15 个 HTML**（prototype.html + 14 个 pages/*.html）的 nav-menu/mobile-menu 统一为 6 项，active 按 data-page 推导。
- view-home.js：学术工具由「人物考据/社会关系/数据统计」改为「人物库/社会关系溯源/数据统计」；新增 Hero CTA（浏览全馆藏 15,694 部 / 人物库 661,350 人）与「趣味探索」板块（随机一书+随机一人，复用 AB.DATA）；头注释 v2.2→v2.3。
- character.html 已是静态引导页（data-page=character-guide，无 view-character.js 引用），与 app/character 一致，本轮仅随 nav 统一。
- 规范文档：更新 `docs/05-设计规范与原型/UI与UX全局设计规范.md`（导航 6 项、人物考据→引导页、学术工具三项）与 `Figma极简原型建模页面清单.md`（Page06 改为人物考据引导页、学术工具三项）。
- 版本落在已存在的 [1.14.3] 小节（package.json 当前=1.14.3，与 CHANGELOG 顶部一致，无需再 bump）。验证：DOM 桩冒烟 8 视图无 undefined；全 15 页 nav=6 校验通过；npm test 39/39 绿。

## 文档去冗余整理（v1.15.1，2026-09-13）
- 用户指令「整理项目文档，去除冗余重复部分」。先派 code-explorer 只读审计全部 docs + 根 README/CHANGELOG + .github 文档，定位冗余簇，再按「单一来源化」（不是删内容，而是把重复块改为指向权威源的指引）。
- 已执行去重：
  - Clu1 项目简介三件套：`docs/01-项目基础说明/README-项目简介.md` 的「核心特性」「快速部署」与根 `README.md` 重复 → 改为指向根 README 的指引。
  - Clu4 代码规范：`.github/CONTRIBUTING.md` §七「代码规范要点」与 `docs/02-架构与开发规范/代码开发规范.md` 重复 → 指向该规范 + 保留提交前必查三条。
  - Clu6 版本日志：`docs/06-部署与迭代/版本迭代管理规范.md` §4 的 CHANGELOG 模板 → 指向根 `CHANGELOG.md`。
  - Clu5 任务清单：`剩余开发任务清单.md` 删除与《开发进度清单》重复的「当前进度核实」状态表 + 「已完成能力概览」，改为单一来源指引；章节三/四/五/六 → 二/三/四/五；P0 标注已完成；统计表增列状态；R27 标记完成。
  - Clu7/Clu8 历史报告归档：`docs/08-技术方案研究/PRD缺陷分析与优化方案.md`、`docs/05-设计规范与原型/原型检查报告与改进建议.md` 加「归档状态」说明（不再作为现行来源）。
  - 《开发进度清单》再刷新至 v1.15.0：47→**53** 用例（9 测试文件，实跑 53/53）；阅读章节补 R1–R4；原型章节改 7 项导航 + `bookmarks.html` 已补；遗留项「原型未同步书签」标完成。
- 有意保留的重叠：`项目整体概述.md`（定位/背景/目标，与 README 仅概念性重叠）；`剩余开发任务清单` §二 与进度清单互补；架构文档「定稿不可变更」故不改其规范正文。
- **再次踩坑**：CHANGELOG 又出现同版本号重复小节（并行会话也用 1.15.1 写「全站页面文案精简」，并同时 bump package.json 至 1.15.1）；已合并为单一 [1.15.1] + 两个子标题（优化 / 文档）。`sync:changelog` 重跑：1.15.1 无落点（未 commit）不生成链接，属正常。
- 结论：并行会话仍高频推进（本轮期间 1.15.0→1.15.1 + 全站文案精简）。任何 bump/写入前必须重读文件；「同版本号重复」是本项目高频风险，写 CHANGELOG 前先搜 `^## \[` 去重。

## 原型页脚精简（续 v1.15.1，2026-09-13）
- 用户指令「更新原型：已有单独统计页面，故去除页脚区的统计文字，去除非必要文字」。原型页脚此前含两块文字：品牌描述行「古籍通 AncientBook · 开源公益古籍阅读与考据平台 | 数据仅供学术参考」+ 统计摘要行（`footer-stats`：馆藏 10 类 / 15,694 部 / 661,350 人 + 「本原型示范 45 部 / 样例人物 38 位」）。
- 一次性脚本（`_tmp_footer.cjs`，已删）清除 **16 个 HTML**（prototype.html + 15 个 pages/*.html，含 v1.15.1 新增的 bookmarks.html）的这两行 `<p>`，仅保留 6 个页脚链接（开源协议/数据来源/免责声明/反馈渠道/数据统计/人物库）。统计信息已由独立 `stats.html` 承载。
- 归属：并入既有 [1.15.1]「全站页面文案精简」小节（补一条原型页脚 bullet + 扩展涉及文件清单含 `prototype/prototype.html`、`prototype/pages/*.html`），不再新开版本（1.15.1 当轮未提交）。`sync:changelog` 重跑补底部链接至 39 个（1.15.1 无落点未生成链接，与 1.14.4 同）。
- 版本：package.json 当前仍 1.15.1（外部会话已 bump），未再升。验证：grep 全仓 prototype HTML 已无 `footer-stats`/品牌行；页脚仅 6 链接。
