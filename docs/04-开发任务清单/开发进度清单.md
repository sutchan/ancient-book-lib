# 古籍通 AncientBook｜开发进度清单

> 核实时间：2026-09-13 · 核实方式：`package.json`、`npm test`（实跑 47/47）、`CHANGELOG.md`、文件清单逐一比对
> 所有数字均取自仓库真实产物与测试输出，未做估算或沿用旧结论。

## 一、版本与仓库状态

| 项 | 当前值 | 状态 |
|---|---|---|
| 版本号 | **1.14.3** | ✅ `package.json` 与 `CHANGELOG.md` 一致（含 [1.14.3] 小节） |
| 工作区 | 存在未提交改动（书签功能实现 + 本轮文档全量同步） | 待提交 |
| 远程同步 | 以 `git status` / `git log` 实查为准 | — |

> 说明：本清单不再固化 HEAD 哈希与提交总数（会话间隙可能被外部提交改动，写死易失真）；需要时以 `git log -1` / `git rev-list --count HEAD` 实查。

## 二、质量信号

| 项 | 结果 |
|---|---|
| 单元测试 | ✅ **47 用例 / 47 通过 / 0 失败**（`npm test`，本轮实测） |
| 测试文件 | 8 个：`bookmarks` / `cbdb-search` / `download` / `p0-fix` / `person-timeline` / `search` / `t2s-extension` / `t2s` |

> 注：v1.14.1 清理考据工作台死代码后用例由 63 降为 39；v1.14.3 新增 `test/bookmarks.test.ts`（+8 用例）后为 **47**。更早文档记录的「39/39」「40/40」均已过时。

## 三、功能完成度清单

### 浏览与检索
- [x] 全馆藏书目浏览 `/catalog`（15,694 部，50 部/页 + 馆藏/子类筛选 + 分页）
- [x] 书目搜索（输入防抖 250ms + `Map` 索引 O(1) 查找）
- [x] 双模式检索（标题检索 + 全文倒排索引；索引缺失自动回退标题）
- [x] 多维筛选（馆藏 / 朝代）
- [x] 书单导出（CSV，含原文直链，零复制）

### 阅读
- [x] 远程原文阅读 `/read/remote`（按需 fetch 上游 raw URL + jsDelivr / statically 镜像降级）
- [x] HTTP Range 分片懒加载（`chapters.json` 字节偏移，避免整本 OOM）
- [x] 繁简双语保真阅读（映射库 820 字）
- [x] 字号 / 行距 / 三主题自定义
- [x] 阅读进度缓存 + 最近阅读 + 已读片段缓存
- [x] 章内分页、划词「检索此句」溯源
- [x] IndexedDB 原文缓存（100MB，LRU 淘汰）
- [x] 大文件保护（>5MB 警告 + 60s 超时中止）

### 书签（v1.14.3 新增）
- [x] 书籍收藏 `/bookmarks`（`lib/bookmarks.ts` + `lib/useBookmarks.ts`，仅 `localStorage`，零后端）
- [x] 导航「书签」入口（顶部导航 7 项）+ 收藏数角标（`id="nav-bookmarks"`）
- [x] 书目卡片星标（`app/catalog/CatalogInner.tsx`）、详情页书签按钮（`app/catalog/book/CatalogBookInner.tsx`）、阅读页收藏切换（`components/RemoteReader.tsx`）
- [x] 阅读位置书签（「保存当前位置」+ 书签列表面板，点击 `goChapter` 恢复章节与页码）
- [x] 导出 / 导入书签 JSON（跨设备迁移，纯文件、无服务器）
- [x] 单测 `test/bookmarks.test.ts`（增删 / 查重 / 序列化容错 / 导入合并 / 阅读位置）

### 人物考据（CBDB 真实数据）
- [x] 全量人物库 **661,350 人** `/people`
- [x] 人物详情（生卒年 / 指数年 / 性别 / 籍贯）
- [x] 多级检索（姓名前缀 → 姓名子串 → 别名字号前缀 → 别名字号子串，简繁互通）
- [x] 亲属关系 561,361 条 + 社会关系 189,938 条 + 著作 50,783 条
- [x] 生平任职（590,540 条 / 298,954 人 / 11,291 种官职）
- [x] 字/號/別名（163,634 条 / 100,585 人）
- [x] 科舉/入仕（264,820 条 / 220,665 人）
- [x] 史料來源（504,591 条 / 452,070 人）
- [x] 生命时间轴 + 关系网络可视化（v1.13）
- [x] 双人关系溯源（分层 BFS，1–3 级）
- [x] 人物库 URL 状态同步（姓氏/朝代/性别/关键词/页码可分享）

### 数据统计
- [x] 概览 KPI + 馆藏分布
- [x] 人物籍贯分布（378,745 人 / 217 个省级行政区）
- [x] 官职-朝代联动（587,506 条 / 12 朝代）
- [x] 科举-朝代分析
- [x] 历代人物时间分布（30.8 万有效指数年 / 20 个世纪区间）

### 体验与合规
- [x] 三主题（日间 / 护眼 / 深色）+ 繁简偏好记忆
- [x] 书签与收藏（书籍收藏 + 阅读位置书签，仅本地缓存，无隐私采集）
- [x] **愉悦体验层「纸墨雅趣」8 触点**（原型 ↔ 正式站双向同步契约）
- [x] 合规四页（开源协议 / 数据来源 / 免责声明 / 反馈渠道）
- [x] GA4 匿名访问统计（IP 匿名化）
- [x] 品牌资料套件 + PWA `site.webmanifest`
- [x] 404 页愉悦化文案与双 CTA

### 原型（prototype/）
- [x] 导航 **6 项**与正式站核心入口一致（首页 / 全馆藏 / 检索 / 人物库 / 社会关系 / 帮助）
- [x] 愉悦层 8 触点同款 CSS（`ink-shimmer` / `card-ink` / `rc-swap` / `scroll-progress` 等）
- [x] 检索空态对齐正式站（「未寻得此卷」+ 可点建议词）
- [ ] **待补**：原型未同步「书签」入口与 `/bookmarks` 演示页（正式站已 7 项，原型仍 6 项）

## 四、遗留与风险清单

| 优先级 | 项 | 说明与建议 |
|---|---|---|
| **P2** | **全文索引 / 章节清单未入库** | `fulltext-index.json`、`chapters.json` 已 gitignore，由构建期生成。生产站（EdgeOne Pages）须将构建命令设为 `npm run build:fulltext && npm run build`，否则全文检索回退标题、Range 分片不生效 |
| **P3** | **原型未同步书签** | 正式站顶部导航已含「书签」（7 项），`prototype/` 仍为 6 项且无 `pages/bookmarks.html`；原型为演示定位，可按需补齐以保持「原型 ↔ 正式站」同步契约 |
| **P3** | 原型 demo 数字 | 原型页脚仍显示「45 部 / 38 人 / 6 关系」，与真实数据（661,350 人）不同——属原型 demo 定位，已有「原型」提示条缓解，未伪造真实数据 |
| **P3** | 馆藏古籍关联人物（规划） | 书目 `CatalogEntry` 无作者字段，「书→人」需构建期以 CBDB `PersonTexts`（角色 撰/著/編/輯/注/箋/疏/述）反向建索引 `public/index/book-persons.json`，运行期 `lib/bookPersons.ts` 按归一化书名查表，详情页新增「关联人物」区块 |

## 五、建议下一步

1. 提交本轮改动（书签功能 + 文档全量同步），版本号同步至 v1.14.4（文档同步）。
2. 按需补齐原型「书签」入口与演示页（P3），维持原型 ↔ 正式站一致性。
3. 规划「馆藏古籍关联人物」：构建期 `scripts/build-book-persons.mjs` + `lib/bookPersons.ts` + `app/catalog/book` 详情页「关联人物」区块 + 单测。
