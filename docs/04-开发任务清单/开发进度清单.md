# 古籍通 AncientBook｜开发进度清单

> 核实时间：2026-09-13 · 核实方式：`package.json`、`npm test`（实跑 69/69）、`tsc --noEmit`（0 错误）、`git log/status/tag`、CHANGELOG、源码扫描（app / components / lib / scripts / test 共 162 个代码文件）
> 所有数字均取自仓库真实产物与测试输出，未做估算或沿用旧结论。

## 一、版本与仓库状态

| 项 | 当前值 | 状态 |
|---|---|---|
| 版本号 | **1.15.8** | ✅ `package.json`（`"version": "1.15.8"`）与 `CHANGELOG.md`（含 `[1.15.8]` 小节）一致 |
| 工作区 | 源码改动已提交（`7259d31 release: v1.15.8`）；本轮「头注释归一化 + 文档同步」未提交 | 待提交 |
| Git 标签 | 最新标签仅至 **v1.14.3**（v1.15.0–v1.15.8 均未打 tag） | ⚠️ 待补打 |
| 远程同步 | 以 `git status` / `git log` 实查为准 | — |

> 说明：本清单不再固化 HEAD 哈希与提交总数（会话间隙可能被外部提交改动，写死易失真）；需要时以 `git log -1` / `git rev-list --count HEAD` 实查。
> 当前 HEAD 为 v1.15.8 发布提交；工作区未提交仅为本轮「源文件头注释归一化至 v1.15.8 + CHANGELOG/文档同步」，无功能性改动。

## 二、质量信号

| 项 | 结果 |
|---|---|
| 单元测试 | ✅ **69 用例 / 69 通过 / 0 失败**（`npm test`，本轮实测） |
| 类型检查 | ✅ `tsc --noEmit` **0 错误**（本轮实测） |
| 测试文件 | 10 个：`bookmarks` / `cbdb-search` / `download` / `fix-regressions` / `p0-fix` / `person-timeline` / `reader-tools` / `search` / `t2s-extension` / `t2s` |

> 注：v1.14.1 清理考据工作台死代码后用例由 63 降为 39；v1.14.3 新增 `test/bookmarks.test.ts`（+8）→ 47；v1.15.0 新增 `test/reader-tools.test.ts`（+6）→ 53；v1.15.5 新增 `test/fix-regressions.test.ts`（+13）→ 66；v1.15.8 在 `search` / `download` / `reader-tools` 各补回归用例（+3）→ **69**。更早文档记录的「39/39」「40/40」均已过时。

## 三、功能完成度清单

### 浏览与检索
- [x] 全馆藏书目浏览 `/catalog`（15,694 部，50 部/页 + 馆藏/子类筛选 + 分页）
- [x] 书目搜索（输入防抖 250ms + `Map` 索引 O(1) 查找）
- [x] 双模式检索（标题检索 + 全文倒排索引；索引缺失自动回退标题）
- [x] 多维筛选（馆藏 / 朝代）
- [x] 书单导出（CSV，含原文直链，零复制）

### 阅读
- [x] 远程原文阅读 `/read/remote`（按需 fetch 上游 raw URL + jsDelivr / statically 镜像降级）
- [x] HTTP Range 分片懒加载（`chapters.json` 字节偏移，避免整本 OOM）
- [x] 繁简双语保真阅读（映射库 800+ 字，v1.15.5 实测）
- [x] 字号 / 行距 / 三主题自定义
- [x] 阅读进度缓存 + 最近阅读 + 已读片段缓存
- [x] 章内分页、划词「检索此句」溯源
- [x] IndexedDB 原文缓存（100MB，LRU 淘汰）
- [x] 大文件保护（>5MB 警告 + 60s 超时中止）
- [x] 阅读器内页面搜索（v1.15.0）：`lib/readerSearch.ts` + `components/ReaderSearchBar.tsx`，命中 `<mark>` 高亮 + 章/页跳转，繁简互通（v1.15.5 修复繁简双向不对称）
- [x] 引用格式一键复制（v1.15.0）：划词浮层「复制引用」生成「《书名·章节》：原文」（`lib/citation.ts`）
- [x] 检索结果定位（v1.15.0）：结果携 `?q=` 跳转阅读页并自动开启页内搜索列出命中
- [x] 移动端阅读优化（v1.15.0）：左右滑动翻页、进度条拖拽跳章、≤420px 小屏字号
- [x] 阅读位置书签键唯一性修复（v1.15.5）：`lib/bookmarks.ts` `removeReadPos` 按严格递增 id 删除，防同毫秒碰撞连带删除

### 书签（v1.14.3 新增）
- [x] 书籍收藏 `/bookmarks`（`lib/bookmarks.ts` + `lib/useBookmarks.ts`，仅 `localStorage`，零后端）
- [x] 导航「书签」入口（顶部导航 7 项）+ 收藏数角标（`id="nav-bookmarks"`）
- [x] 书目卡片星标、详情页书签按钮、阅读页收藏切换
- [x] 阅读位置书签（「保存当前位置」+ 书签列表面板，点击 `goChapter` 恢复章节与页码）
- [x] 导出 / 导入书签 JSON（跨设备迁移，纯文件、无服务器）
- [x] 单测 `test/bookmarks.test.ts`（增删 / 查重 / 序列化容错 / 导入合并 / 阅读位置）

### 人物考据（CBDB 真实数据）
- [x] 全量人物库 **661,350 人** `/people`（v1.15.5 修复 39,850 人/6.03% 此前不可达：meta 截断已移除，CI `--check` 守卫）
- [x] 人物详情（生卒年 / 指数年 / 性别 / 籍贯）
- [x] 多级检索（姓名前缀 → 姓名子串 → 别名字号前缀 → 别名字号子串，简繁互通）
- [x] 亲属关系 + 社会关系 + 著作（v1.15.5 实测：`meta.total = 661,350`，`648,691 + 12,659` 自校验通过）
- [x] 生平任职、字/號/別名、科舉/入仕、史料來源
- [x] 生命时间轴 + 关系网络可视化（`components/RelationGraph.tsx`）
- [x] 双人关系溯源（v1.15.5 修复张冠李戴：详情页改传权威 `?id=<CBDB ID>`）
- [x] 人物库 URL 状态同步（姓氏/朝代/性别/关键词/页码可分享）

### 数据统计
- [x] 概览 KPI + 馆藏分布
- [x] 人物籍贯分布（378,745 人 / 217 个省级行政区）
- [x] 官职-朝代联动、科举-朝代分析、历代人物时间分布

### 体验与合规
- [x] 三主题（日间 / 护眼 / 深色）+ 繁简偏好记忆
- [x] 书签与收藏（仅本地缓存，无隐私采集）
- [x] 愉悦体验层「纸墨雅趣」8 触点
- [x] 合规四页（开源协议 / 数据来源 / 免责声明 / 反馈渠道）
- [x] GA4 匿名访问统计（IP 匿名化）
- [x] 品牌资料套件 + PWA `site.webmanifest`（⚠️ 仅 manifest，无 Service Worker，离线缓存未实现，见遗留清单）
- [x] 404 页愉悦化文案与双 CTA

### 原型（prototype/）
- [x] 导航 **7 项**与应用端一致
- [x] `prototype/pages/bookmarks.html`（我的书架静态演示）
- [x] 愉悦层 8 触点同款 CSS、检索空态对齐正式站

## 四、遗留与风险清单

| 优先级 | 项 | 说明与建议 |
|---|---|---|
| **P1** | **Git 标签 / 推送待补** | v1.15.5–v1.15.8 代码与文档均已 commit（HEAD 为 `7259d31 release: v1.15.8`），但 v1.15.0–v1.15.8 均未打 tag，且 `main` 可能领先 `origin/main`，建议补打 tag 并推送 |
| **P2** | **Git 标签缺失** | 最新标签 `v1.14.3`，v1.15.0–v1.15.8 均未打 tag；`剩余开发任务清单.md` R24 的推送动作亦未执行 |
| **P2** | **全文索引 / 章节清单未入库** | `fulltext-index.json`、`chapters.json` 已 gitignore，由构建期生成。生产站须将构建命令设为 `npm run build:fulltext && npm run build`，否则全文检索回退标题、Range 分片不生效 |
| **P3** | PWA 离线不支持 | 仅有 `site.webmanifest`，无 Service Worker；R11（离线可读 + 添加到主屏幕）未实现 |
| **P3** | 原型演示数据 | 原型页脚统计摘要行（含「45 部 / 38 人」）已于 v1.15.1 移除；原型书目 / 人物为演示数据，与真实站不同源，已有「原型」提示条说明 |
| **P3** | 馆藏古籍关联人物（规划） | 书目 `CatalogEntry` 无作者字段，「书→人」需构建期以 CBDB `PersonTexts` 反向建索引 `public/index/book-persons.json`，运行期 `lib/bookPersons.ts` 查表，详情页新增「关联人物」区块 |

## 五、建议下一步

1. 补打 tag `v1.15.5`–`v1.15.8` 并推送（当前标签止于 v1.14.3）；推送前先核对 `main` 与 `origin/main` 差异，注意历史中曾误入库的本地记忆文件需先处理。
2. 剩余任务以 [`剩余开发任务清单.md`](./剩余开发任务清单.md) 为准：P1 批注 / 笔记 / 专题文集 / 名句典故库（R6–R9）为下一版本候选，源码实测均尚未实现。
3. 规划「馆藏古籍关联人物」「PWA 离线」等 P2/P3 项，按成本排期。
